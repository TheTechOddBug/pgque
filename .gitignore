# Generated output from build/transform.sh
build/output/

# Client build artifacts
clients/python/*.egg-info/
clients/typescript/node_modules/
clients/typescript/package-lock.json

# Claude Code agent worktrees (ephemeral isolation per agent run)
.claude/worktrees/

# Website (web/) build artifacts
web/node_modules/
web/dist/
web/.astro/
# web/bun.lock is committed on purpose (reproducible Bun installs).
# Ignore any stale npm lockfile now that the site uses Bun.
web/package-lock.json
